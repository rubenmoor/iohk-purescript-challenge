{
    "private": true,
    "scripts": {
        "build": "pulp browserify --optimise --to site/main.js && cp -r site ../server"
    },
    "dependencies": {
        "virtual-dom": "^2.1.1"
    },
    "devDependencies": {
        "pulp": "^10.0.0"
    },
    "repository": {
        "type": "git",
        "url": "git://github.com/rubenmoor/iohk-purescript-challenge.git"
    }
}
